name: qingkuai
fileTypes: [qk]
scopeName: source.qk-emmet
uuid: 762041dd-c0dd-4e04-b8ff-996bf64c76f2

patterns:
  # emmet提示中的嵌入语言语法规则：标签中的内容都会被认作注释，这样处理是为了让模拟光标的字符显示为灰色
  # embedded language block for emmet: everything in embedded block is considered a comment
  # This is done so that the characters that simulate the cursor in the completion suggestion prompt are grayed out
  - begin: (?=<lang-js)
    end: (?=</lang-js)
    patterns:
      - include: "source.qk#tag-structure-start"
      - name: comment.block.qk-emmet
        begin: (?<=>)
        end: (?=</lang-js)

  - begin: (?=<lang-ts)
    end: (?=</lang-ts)
    patterns:
      - include: "source.qk#tag-structure-start"
      - name: comment.block.qk-emmet
        begin: (?<=>)
        end: (?=</lang-ts)

  - begin: (?=<lang-css)
    end: (?=</lang-css)
    patterns:
      - include: "source.qk#tag-structure-start"
      - name: comment.block.qk-emmet
        begin: (?<=>)
        end: (?=</lang-css)

  - begin: (?=<lang-asss)
    end: (?=</lang-asss)
    patterns:
      - include: "source.qk#tag-structure-start"
      - name: comment.block.qk-emmet
        begin: (?<=>)
        end: (?=</lang-asss)

  - begin: (?=<lang-scss)
    end: (?=</lang-scss)
    patterns:
      - include: "source.qk#tag-structure-start"
      - name: comment.block.qk-emmet
        begin: (?<=>)
        end: (?=</lang-scss)

  - begin: (?=<lang-less)
    end: (?=</lang-less)
    patterns:
      - include: "source.qk#tag-structure-start"
      - name: comment.block.qk-emmet
        begin: (?<=>)
        end: (?=</lang-less)

  - begin: (?=<lang-stylus)
    end: (?=</lang-stylus)
    patterns:
      - include: "source.qk#tag-structure-start"
      - name: comment.block.qk-emmet
        begin: (?<=>)
        end: (?=</lang-stylus)

  - begin: (?=<lang-postcss)
    end: (?=</lang-postcss)
    patterns:
      - include: "source.qk#tag-structure-start"
      - name: comment.block.qk-emmet
        begin: (?<=>)
        end: (?=</lang-postcss)

  - include: "source.qk"
